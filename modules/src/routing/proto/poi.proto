syntax = "proto2";

package EDrive.routing;

import "routing/proto/routing.proto";

message Landmark {
  optional string name = 1;
  repeated LaneWaypoint waypoint = 2;
}

message POI {
  repeated Landmark landmark = 1;
}